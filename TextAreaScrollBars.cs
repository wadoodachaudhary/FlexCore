namespace Fx.ControlKit;

/// <summary>Native scrollbar modes supported by <see cref="TextAreaControl"/>.</summary>
public enum TextAreaScrollBars
{
    Auto,
    None,
    Horizontal,
    Vertical,
    Both
}
